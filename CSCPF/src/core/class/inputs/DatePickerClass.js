class DatePickerClass {
  constructor(id, transition, prependIcon, rules) {
    this.id = id;
    this.transition = transition;
    this.prependIcon = prependIcon;
    this.rules = rules;
  }
}

export default DatePickerClass;
